package com.salessystem;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class SalesSystemApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the Spring context starts up without configuration errors.
        // Business rule tests (CPF, duplicates, etc.) are added in Project 3.
    }
}
